cmake_minimum_required(VERSION 3.17)
project(STB)

file(GLOB_RECURSE STB_HEAD *.h)
if(WIN32)
    add_library(stb INTERFACE ${STB_HEAD})
else()
    add_library(stb ${STB_HEAD})
endif()
set_target_properties(stb PROPERTIES LINKER_LANGUAGE C)
